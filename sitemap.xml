---
layout: null
---
<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.sitemaps.org/schemas/sitemap/0.9
        http://www.sitemaps.org/schemas/sitemap/0.9/sitemap.xsd">

  <!-- ── Static pages ─────────────────────────────────────── -->
  {% for page in site.html_pages %}
    {% unless page.url contains '/404' or page.url contains '/group/' %}
    <url>
      <loc>{{ site.url }}{{ page.url | remove: "index.html" }}</loc>
      <lastmod>{{ site.time | date_to_xmlschema }}</lastmod>
      <changefreq>{% if page.url == '/' %}weekly{% else %}monthly{% endif %}</changefreq>
      <priority>
        {%- if page.url == '/' -%}1.0
        {%- elsif page.url contains '/business/' -%}0.9
        {%- elsif page.url contains '/company/' or page.url contains '/about/' -%}0.85
        {%- elsif page.url contains '/works/' -%}0.8
        {%- elsif page.url contains '/contact/' or page.url contains '/download/' -%}0.85
        {%- elsif page.url contains '/news/' -%}0.7
        {%- else -%}0.6
        {%- endif -%}
      </priority>
    </url>
    {% endunless %}
  {% endfor %}

  <!-- ── News collection ──────────────────────────────────── -->
  {% assign sorted_news = site.news | sort: "date" | reverse %}
  {% for item in sorted_news %}
  <url>
    <loc>{{ site.url }}{{ item.url }}</loc>
    <lastmod>{{ item.date | date_to_xmlschema }}</lastmod>
    <changefreq>monthly</changefreq>
    <priority>0.75</priority>
  </url>
  {% endfor %}

</urlset>
